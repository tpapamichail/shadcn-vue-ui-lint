// A future cn: one class group uses a validator this linter has never
// heard of. Built on the real cn installed for the package.
const real = require("../../../../../node_modules/cn/dist/config.cjs")
exports.defaultConfig = () => {
  const config = real.defaultConfig()
  config.classGroups["future"] = [{ future: [{ $v: "isFutureThing" }] }]
  return config
}
exports.mergeConfigs = real.mergeConfigs
