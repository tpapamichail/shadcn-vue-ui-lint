---
"evals": patch
---

Retarget the eval pipeline to Vue: `.vue` fixtures, Vue-native generation and correction prompts, `vue-eslint-parser` lint pass, SFC-aware component validation, and Vue-based fidelity rendering.
